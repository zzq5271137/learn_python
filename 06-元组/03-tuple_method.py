"""
元组常用方法
"""

# count(): 获取元组中某个值(传入的参数)出现的次数
print("count()")
tuple1 = (1, 4, 3, 2, 3)
print(tuple1.count(3))

print("#########################################################")

# index(): 获取元组中某个元素(传入的参数)的下标, 如果有多个, 则返回首次出现的下标
print("index()")
tuple2 = (1, 4, 3, 2, 3)
print(tuple2.index(3))
